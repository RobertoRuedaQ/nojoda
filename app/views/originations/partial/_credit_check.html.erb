<% if !current_application.nil? && !current_company.credit_check_disclosure_id.nil? %>
	<div class="card">
		<div class="card-body">
			<%= create_legal_document_form( current_company.credit_check_disclosure_id, hidden_fields: create_hidden_values_application( current_application.id),disabled: current_application.document_signed?(current_company.credit_check_disclosure_id),value: (current_application.document_signed?(current_company.credit_check_disclosure_id) ? current_application.legal_match.kept.where(legal_document_id: current_company.credit_check_disclosure_id).first.answer : nil) ) %>
		</div>
	</div>
<% end %>
