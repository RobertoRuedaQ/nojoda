<%= render 'students/partial/students_header' %>


<div class="nav-tabs-top">
  <%= render 'students/partial/tabs' %>

  <div class="tab-content bg-light">
    <div class="tab-pane fade show active" id="collection-tab">
      <div class="card">
        <div class="card-body">
          <%= render "students/partial/reference_information" %>

          <br>

          <%= createTable(@student.collection, student_collection_template(@student.collection) , id: 'table_history_collection_track',title: false,alternative_new_link: new_collection_path(user_id: @student.id)).html_safe %>
        </div>
      </div>
    </div>

  </div>
</div>


